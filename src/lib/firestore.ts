// Firestore is gone. Everything is in ./data on the BizTech SDK. This file exists only so a
// stale import fails with a clear message instead of a mystery.
throw new Error("@/lib/firestore was removed; import from @/lib/data instead");
export {};
